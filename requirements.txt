# ── PyTorch CPU index (must be BEFORE torch) ──────────────────────────────────
--extra-index-url https://download.pytorch.org/whl/cpu

# ── Streamlit web app ─────────────────────────────────────────────────────────
streamlit>=1.35.0
streamlit-folium>=0.20.0

# ── Satellite + climate data ──────────────────────────────────────────────────
earthengine-api>=0.1.390

# ── ML — CPU-only torch (no CUDA = ~250 MB instead of 2 GB, fixes Railway OOM)
torch>=2.0.0
numpy>=1.24.0
scikit-learn>=1.4.0

# ── Data manipulation (you will need this for CSV batch mode and feature engineering)
pandas>=2.0.0

# ── Map rendering ─────────────────────────────────────────────────────────────
folium>=0.15.0

# ── REST API ──────────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
slowapi>=0.1.9
httpx>=0.27.0
python-multipart>=0.0.9    # ← for GeoJSON file uploads in /v1/predict/batch

# ── Environment / utils ───────────────────────────────────────────────────────
python-dotenv>=1.0.0         # ← loads .env for local dev (docker-compose uses it too)

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.2.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
pytest-mock>=3.14.0

# ── Optional: plotting & geospatial ───────────────────────────────────────────
plotly>=5.22.0          # for NDVI time-series charts (mentioned in roadmap)
geopandas>=0.14.0       # for GeoJSON field boundary uploads
shapely>=2.0.0          # geospatial geometry operations